# Home assistant config - v0.3.0
# 2019-02-24T16:30

# Backend
homeassistant:
  name: Home
  latitude: !secret home_lat
  longitude: !secret home_long
  elevation: !secret home_elev
  unit_system: imperial
  time_zone: !secret time_zone
  customize: !include customize.yaml

updater:
  include_used_components: true

custom_updater:
  track:
    - cards
    - components
    - python-scripts
  card_urls:
    - https://raw.githubusercontent.com/custom-cards/information/master/repos.json

mqtt:
    broker: !secret mqtt_ip
    discovery: true
    discovery_prefix: homeassistant

influxdb:
  host: !secret influx_host
  database: home_assistant
  username: !secret influx_username
  password: !secret influx_password

group: !include groups.yaml

automation: !include automations.yaml

script: !include scripts.yaml

# Web Interface
http:
  base_url: !secret base_url
  ssl_certificate: !secret ssl_cert
  ssl_key: !secret ssl_key

duckdns:
  domain: !secret duckdns_domain
  access_token: !secret duckdns_token

frontend:
  themes: !include_dir_merge_named themes
  extra_html_url:
    - /local/custom-fa/solid.html
    - /local/custom-fa/regular.html
    - /local/custom-fa/brands.html

lovelace:
  mode: yaml

panel_iframe: !include config/iframe.yaml

conversation:

tts: !include config/tts.yaml

recorder:
    purge_keep_days: 5
    purge_interval: 1

map:

logbook:

history:

config:

system_health:

ios: !include config/ios.yaml

mobile_app:

homekit:

notify: !include config/notify.yaml

# Map Zones
zone: !include config/zone.yaml

# Sensors
sun:

weather:
  - platform: darksky
    api_key: !secret darksky_api

speedtestdotnet: !include config/speedtest.yaml

sensor: !include config/sensor.yaml

binary_sensor: !include config/binary_sensor.yaml

switch: !include config/switch.yaml

light: !include config/light.yaml

input_boolean: !include config/input_boolean.yaml

device_tracker: !include config/device_tracker.yaml

person:

shopping_list:

apple_tv: !include config/apple_tv.yaml

media_player: !include config/media_player.yaml

alexa_media:
  accounts:
    - email: !secret alexa_email
      password: !secret alexa_pass
      url: !secret alexa_url

geo_location: !include config/geo_location.yaml

aqualink:
  username: !secret pool_user
  password: !secret pool_pass

# Alarm
alarm_control_panel: !include config/alarm.yaml
